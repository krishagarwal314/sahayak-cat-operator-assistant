# Offline demo: no voice or language models, no torch. About 130 MB to install.
# Voices come from the recordings in backend/voices/, the web app is prebuilt in
# frontend/dist/, the small ML models are prebuilt in backend/models/ml/.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7
PyJWT>=2.8
numpy>=1.26,<2
scikit-learn==1.7.2
pandas>=2.0
joblib>=1.3
# face login (its two small models ship in backend/models/face/)
opencv-python-headless>=4.8,<4.12
