# ---- web layer ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
PyJWT>=2.8

# ---- ML runtime ----
# torch>=2.6 is REQUIRED, not just preferred: the Hindi Whisper checkpoint and
# MuRIL ship pickle (.bin) weights with no safetensors copy, and transformers
# refuses to torch.load those on older torch. Install for your platform first:
#   CPU  : pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA : pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.6
transformers>=4.44
sentencepiece>=0.2
accelerate>=0.33
safetensors>=0.4
# numpy<2: many preinstalled stacks (scipy 1.11, pandas 2.1, matplotlib 3.8)
# are built against numpy 1.x and break on import under numpy 2.
numpy>=1.26,<2
scipy>=1.11
huggingface_hub>=0.24

# ---- face login ----
# YuNet + SFace ship inside OpenCV; headless avoids pulling in GUI libraries.
# <4.12: from 4.12 OpenCV requires numpy>=2, which breaks the pin above.
opencv-python-headless>=4.8,<4.12

# ---- audio ----
soundfile>=0.12
librosa>=0.10

# ---- intent classifier training (optional, only needed for train.py) ----
scikit-learn>=1.4
datasets>=2.20

# ---- optional upgrades -------------------------------------------------
# IndicTrans2 tokenizer/postprocessor (TRANSLATE_MODEL=ai4bharat/indictrans2-*)
#   pip install IndicTransToolkit
# IndicF5 text-to-speech (TTS_MODEL=ai4bharat/IndicF5)
#   pip install f5-tts
